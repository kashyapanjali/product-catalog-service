apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-catalog-v2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-catalog
      version: v2
  template:
    metadata:
      labels:
        app: product-catalog
        version: v2
    spec:
      containers:
      - name: product-catalog
        image: your-docker-repo/product-catalog:v2
        ports:
        - containerPort: 3000 